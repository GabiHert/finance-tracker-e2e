# Finance Tracker E2E - Environment Configuration Template
# Copy this file to .env.e2e and update paths for your environment

# =============================================================================
# Project Paths (REQUIRED - Update these for your environment)
# =============================================================================
BACKEND_PATH=/path/to/finance-tracker/backend
FRONTEND_PATH=/path/to/finance-tracker/frontend
INFRA_PATH=/path/to/finance-tracker/infra
E2E_PATH=/path/to/finance-tracker/e2e

# =============================================================================
# E2E Infrastructure Ports (Different from dev to avoid conflicts)
# =============================================================================
POSTGRES_E2E_PORT=5434
MINIO_E2E_API_PORT=9002
MINIO_E2E_CONSOLE_PORT=9003
REDIS_E2E_PORT=6380
BACKEND_E2E_PORT=8081
FRONTEND_E2E_PORT=3001

# =============================================================================
# E2E Database Configuration
# =============================================================================
POSTGRES_E2E_USER=e2e_user
POSTGRES_E2E_PASSWORD=e2e_password
POSTGRES_E2E_DB=finance_tracker_e2e

# =============================================================================
# E2E Redis Configuration
# =============================================================================
REDIS_E2E_PASSWORD=e2e_redis_password

# =============================================================================
# E2E MinIO Configuration
# =============================================================================
MINIO_E2E_ROOT_USER=e2e_minio_admin
MINIO_E2E_ROOT_PASSWORD=e2e_minio_password
MINIO_E2E_BUCKET=finance-tracker-uploads

# =============================================================================
# E2E JWT Configuration
# =============================================================================
JWT_E2E_SECRET=e2e-jwt-secret-for-testing
JWT_E2E_EXPIRY=15m
REFRESH_TOKEN_E2E_EXPIRY=7d

# =============================================================================
# Playwright Configuration
# =============================================================================
PLAYWRIGHT_BASE_URL=http://localhost:3001
PLAYWRIGHT_API_URL=http://localhost:8081/api/v1

# =============================================================================
# E2E Test User Credentials
# These credentials are used for auth.setup.ts to create authenticated sessions
# Note: Password avoids special characters that can cause shell escaping issues
# =============================================================================
E2E_TEST_USER_EMAIL=e2e-test@example.com
E2E_TEST_USER_PASSWORD=E2eTestPassword1234
